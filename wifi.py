#!/usr/bin/env python

# Note: This script runs Wifi from within a cloned git repo.
# The script `bin/wifi` is designed to be run after installing (from /usr/sbin), not from the cwd.

from wifi import wifi
wifi.main()
